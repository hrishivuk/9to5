export type LogEntry={id:number;round:number;text:string;detail:string};
export default function BattleLog({entries}:{entries:LogEntry[]}){return <section className="battle-log"><div className="log-heading"><span>▤ COMBAT RECEIPTS</span><span>LIVE FEED <i/></span></div><div className="log-content" role="log" aria-live="polite" aria-relevant="additions">{entries.slice(-3).reverse().map((e,i)=><div className={`log-entry ${i===0?'latest':''}`} key={e.id}><span className="log-round">R{String(e.round).padStart(2,'0')}</span><p>{e.text} <small>{e.detail}</small></p></div>)}</div></section>}
