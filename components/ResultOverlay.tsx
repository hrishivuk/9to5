import type {BattleState} from '@/lib/battle';
export default function ResultOverlay({state,onRematch,onTitle}:{state:BattleState;onRematch:()=>void;onTitle:()=>void}){const win=state.result==='win',draw=state.result==='draw';return <div className="result-backdrop"><section role="dialog" aria-modal="true" aria-labelledby="result-title" className="result-panel"><span className="eyebrow">{win?'PERFORMANCE REVIEW: EXCEEDS EXPECTATIONS':draw?'MUTUAL BURNOUT':'A NOTE FROM HUMAN RESOURCES'}</span><div className="result-stamp">{win?'★':draw?'☕':'!'}</div><h2 id="result-title">{win?'PROMOTED':draw?'EVERYONE CLOCKED OUT':'PUT ON A PIP'}</h2><p>{win?'The Scrum Lord has been successfully managed.':draw?'The meeting has no survivors.':'Performance concerns have been raised.'}</p><div className="result-stats"><div><strong>{state.round}</strong><span>ROUNDS SURVIVED</span></div><div><strong>{state.player}</strong><span>MENTAL CAPACITY</span></div></div><p className="finisher">FINISHING MOVE <b>{state.finisher||'Corporate bureaucracy'}</b></p><button autoFocus className="primary-button" onClick={onRematch}>REMATCH <span>↗</span></button><button className="text-button" onClick={onTitle}>BACK TO TITLE</button><small className="satire">Fictional workplace. Suspiciously familiar problems.</small></section></div>}
