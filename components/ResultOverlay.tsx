import type {BattleState} from '@/lib/battle';
import type {FighterProfile} from '@/lib/fighter';
export default function ResultOverlay({state,onRematch,onContinue,fighter}:{state:BattleState;onRematch:()=>void;onContinue:()=>void;fighter:FighterProfile}){const win=state.result==='win',draw=state.result==='draw';return <div className="result-backdrop"><section role="dialog" aria-modal="true" aria-labelledby="result-title" className="result-panel"><span className="eyebrow">{win?'PERFORMANCE REVIEW: EXCEEDS EXPECTATIONS':draw?'MUTUAL BURNOUT':'A NOTE FROM HUMAN RESOURCES'}</span><div className="result-stamp">{win?'★':draw?'☕':'!'}</div><h2 id="result-title">{win?'PROMOTED':draw?'EVERYONE CLOCKED OUT':'PUT ON A PIP'}</h2><p>{win?`${fighter.name} has successfully managed The Scrum Lord.`:draw?'The meeting has no survivors.':'Performance concerns have been raised.'}</p><div className="result-stats"><div><strong>{state.round}</strong><span>ROUNDS SURVIVED</span></div><div><strong>{state.player}</strong><span>MENTAL CAPACITY</span></div></div><p className="finisher">FINISHING MOVE <b>{state.finisher||'Corporate bureaucracy'}</b></p><button autoFocus className="primary-button" onClick={onContinue}>{win?'CLAIM YOUR COMPENSATION':'END SHIFT'} <span>↗</span></button><button className="text-button" onClick={onRematch}>REMATCH</button><small className="satire">Fictional workplace. Suspiciously familiar problems.</small></section></div>}
